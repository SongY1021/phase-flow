---
name: phase-handoff
description: 【PhaseFlow】当某个技术层执行中断（上下文接近上限、主动暂停、异常终止）时，生成层级维度的 handoff 文件，记录执行状态以供新会话精确恢复。用户说"中断了"、"需要暂停"、"上下文快满了"、"生成 handoff"时触发。正常完成的技术层不需要手动触发（由 /phase-contract 承接）。
---

# PhaseFlow — phase-handoff

记录技术层的执行中断状态，使新会话能从精确断点恢复，而不是从头重来。

## 核心定位

handoff 解决的是**中断恢复**问题，不是契约传递问题。
契约传递由 `/phase-contract` 负责；handoff 只在中断发生时有意义。

正常完成的技术层：执行 `/phase-contract` 即可，不需要 handoff。
中断的技术层：先执行 `/phase-handoff`，新会话恢复后继续，完成后再执行 `/phase-contract`。

## 两种触发场景

**场景 A：主动中断**（开发者察觉上下文接近 50% 时主动触发）
**场景 B：被动中断**（上下文超限或其他异常，在能操作的最后时刻触发）

建议：上下文使用超过 40% 时即主动触发，不要等到被迫中断。

## 使用方式

```
/phase-handoff {N} {layer}
```

示例：`/phase-handoff 2 service`（Phase 2 服务层中断）

## 执行步骤

### Step 1：快速盘点当前状态

在生成文件前，先在对话中输出一份简短的状态确认，让开发者核对：

```
当前中断状态确认：
- Phase：{N} {模块名}
- Layer：{layer}
- 已完成任务：{列表}
- 未完成任务：{列表}
- 已修改文件：{列表}

确认无误后生成 handoff 文件。
```

### Step 2：生成 handoff 文件

输出路径：`docs/handoffs/handoff-{模块名}-{layer}-partial.md`

```markdown
# Handoff: {模块名} - {技术层}（中断）

> Phase {N} | 中断时间：{date}
> 状态：partial

## 已完成任务
- {精确到方法/类级别的完成项}
- 测试状态：{通过 N 个 / 失败 M 个 / 未运行}

## 未完成任务
- {剩余任务列表，按优先级排序}

## 已修改文件
- {文件路径} — {修改内容简述}

## 接续点
新会话开始后，从以下位置继续：
{精确描述：哪个文件、哪个方法、做到什么程度}

## 已知问题
{中断前发现但未处理的问题，如有}
```

### Step 3：输出新会话恢复模板

生成文件后，直接输出以下内容供开发者复制：

```
========== 新会话恢复模板（直接复制使用）==========

我正在继续一个中断的开发任务，请先阅读以下文件：

1. ROADMAP.md
2. PLAN.md
3. docs/arch-review.md
4. docs/api/{模块名}.md
5. docs/handoffs/handoff-{模块名}-{layer}-partial.md

阅读完成后，告诉我你理解到的当前状态和接续点，不要开始写代码。

=====================================================
```

### Step 4：提示关闭当前会话

```
handoff 文件已生成：docs/handoffs/handoff-{模块名}-{layer}-partial.md

请复制上方的新会话恢复模板，关闭当前会话后开启新会话粘贴使用。
新会话恢复后继续完成本层，完成后执行 /phase-contract {N} {layer}。
```
